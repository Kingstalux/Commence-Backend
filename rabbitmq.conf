# Allow guest user to connect from localhost
loopback_users = none

# Additional configuration for Windows Docker compatibility
default_user = guest
default_pass = guest
default_vhost = /

# Allow guest user to connect from any host
loopback_users.guest = false
